# Development and testing dependencies only — NOT installed in the
# production Docker image (Dockerfile.prod installs requirements.txt
# only). Install both for local development:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
pytest-flask==1.3.0
pyflakes==3.2.0
flake8==7.1.1
