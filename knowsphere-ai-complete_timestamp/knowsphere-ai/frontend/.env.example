# Copy to frontend/.env when running the frontend outside Docker (npm run dev).
# Vite only reads .env files from within frontend/, not the project root.
VITE_API_BASE_URL=http://localhost:5000/api/v1
